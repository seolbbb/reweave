# Windows product completion execution contract

Date: 2026-09-09. Status: active, not product completion.
Origin: owner execution request from task `01a0866f-063f-7ee0-8419-53930ec8d1c7`.
Active Goal: `01a0869b-0053-7630-9d91-9d212f60ce2b`, without a token budget.
This preserves the full execution scope beyond the Goal tool's 4,000-character limit.
Canonical responsibility stays with PRODUCT_SPEC, ROADMAP, PROJECT_STATUS and DECISION_LOG;
this is an execution and acceptance ledger, not a replacement product contract.

## Outcome and scope preservation

Finish a Windows product the owner can actually continue using, not a plan or partial visual
refresh. Reweave is a local Context Library for explicitly saved AI conversations, important
context, decisions and learning with inspectable sources and later ChatGPT/Claude reuse.
The value is less manual classification, item/document selection and repeated explanation.
Context Home delivers the first value; requested reuse in existing web AI delivers the second.
The entire currently approved Product Spec remains in scope. Implement the core first, then
all remaining accepted behavior. Do not rename a reduced MVP as completion. If a real reason
requires scope/direction change, explain evidence, user impact and choices before changing it.

Preserve local-first, BYOK, evidence, explicit Save/Use, sensitive information and sharing scope.
No ambient collection, unselected chat reads, unauthorized transmission, silent injection or
automatic submission. Product UI, repository comments/docs/commits/PRs stay English. Explain
progress and decisions to the owner in clear Korean. Preserve unrelated user work and data.

## Authority and continuity

The user delegates technical choices, reversible fixes, tests and the AGENTS delivery loop.
Do not stop after a plan or repeatedly ask routine permission. Before nontrivial changes/new
problems, state observed evidence, meaningful options, selected approach and verification.
Mark changed decisions superseded, preserve rationale, and continue independent work while
any genuinely required user answer is pending. Keep the original Goal across compactions.

Full interview routes are settled: intent/audience, success, values, scope, UX and data/trust
are answered by the execution request and Product Spec; implementation/compatibility/testing
are agent-owned; public scope freeze and encrypted sync timing are deferred to the owner at
public-release preparation; new paid transmission and actual desktop interaction need their
specified future approval. No new account, pricing, distribution or public publication is authorized.

The user requested GPT-6 Astra, ultra reasoning and speed x2. The source task reported host
priority service but creation has no speed field. Do not claim verified x2, change global
settings, or stop product work over it.

## Observe before implementing

Read actual AGENTS, then Product Spec, Status, Roadmap, relevant Decisions. Use installed
manage-project-intent and requested `C:/Users/Seongbeom/.codex/skills/ui-ux-pro-max/SKILL.md`.
Re-observe code, Git, tests, runtime and documentation drift. Prior tests/checkmarks are historical.
The initial report separates functioning implementation, unverified behavior, missing/broken
flows, first-use obstacles, UI problems, execution sequence and user acceptance at each stage.

Initial code audits: no Import-to-Context analysis controls/queue wiring; reanalysis may delete
item history; missing later canonical spaces/corrections/trust/retrieval/graph; plaintext backup;
hard page limits; legacy Ask/Audit/Reports navigation. These are code observations, not fresh
runtime evidence. Initial Git was clean at `2303357`; the worktree started detached.

## Do not disturb the active Windows computer

- No physical mouse/keyboard/clipboard, Computer Use, visible native windows, focus stealing,
  active browser/profile automation, or terminating the owner's apps without separate permission.
- Use isolated REWEAVE_DATA_DIR, test DB, empty dedicated profiles and ports. Also isolate keyring,
  Native Messaging registration, runtime descriptor and autostart; changing only DB is insufficient.
- Start with synthetic data and test providers through real product paths. Never substitute
  hardcoded fake product outcomes for a working implementation.
- Use headless/background browsers and API/storage checks; bound server/process count and load.
  Clean only owned processes. Do not use registry registration scripts during background QA.
- Use an isolated Windows environment if actually available. Native install/account sign-in
  evidence that cannot be proven in background stays explicitly unverified. Prepare executable
  and minimal user steps first. Never bypass a blocked test with unauthorized desktop automation.
- Existing code shares keyring service `Reweave`, tests can resolve default app paths, and desktop
  startup always opens pywebview. Fix/guard these paths before executing affected checks.

## Real-data and cost boundary

DEC-026 moves PHASE-002/TASK-005 real-data evidence from implementation prerequisite to final
mandatory acceptance. Record supersession before implementation. Synthetic functionality, UI
and recovery tests may proceed; they never establish actual utility or real-data completion.
Only explicitly selected/authorized owner conversations may be used. Never copy raw source,
Context content or API keys into chat, repository or public logs.

Before paid LLM calls or actual conversation transmission, finish preparatory work, then present
the model, transmitted data scope, maximum calls, cost ceiling and failure stop conditions.
No such paid-call approval currently exists. Do not ask again for already granted approval.
Do not retry an identical failure indefinitely. Continue independent work while approval waits.

## Selected visual contract: A — Reading Room

The owner selected A on 2026-09-09; never reopen A/B/C selection. Source folder:
`C:/Users/Seongbeom/.codex/visualizations/2026/09/09/01a0866f-063f-7ee0-8419-53930ec8d1c7/reweave-uiux-draft`.
Required records: direction-a.png (actually inspect via view_image), design-system/MASTER.md,
design-system/pages/context-home.md, tokens.json (`selectedDirection: A`), DESIGN_BRIEF.md,
GOAL_HANDOFF_A.txt, baseline-home.png, baseline-explorer.png and index.html. Old GOAL_HANDOFF.txt
is preselection and must not be applied. Preserve originals; approved artifacts may be copied
into docs/design/reading-room. Never copy baseline-data or its server into product data.

- Canvas #F7F6F2, surface #FFFFFF, text #242A28, muted #5E6762, action #31584B,
  selected #E9F0EB, divider #DCDDD6, danger #A63434. Shared semantic tokens/components.
- Sans-serif Inter/Segoe UI body 16px/1.55. Newsreader-like serif only for short headings,
  generally 30–32px at normal desktop size. Offline fonts/fallbacks; written rules override
  oversized image titles and serif body artifacts. Do not add runtime font requests.
- Home, Explore, Sources; persistent Search; footer Settings; explicit Import. Home is a primary
  reading document with a narrow companion area stacking at smaller widths. Explore is efficient
  list/detail in the same A theme. Graph is secondary Explore, Review is exceptions.
- Retire user-facing Ask Archive/Memory Audit/Reports per spec while preserving saved data and
  reusable internal functionality. Real UI/data only, no screenshot background or fake metrics.
- Put evidence and observed/inferred/suggested distinction near relevant reading. Desktop
  "How to use in chat" explains the extension; never secretly inserts, analyzes, saves or submits.
- Apply to onboarding, Import, extension setup, BYOK, Home/Explore/search/source details,
  analysis queue/progress/failure/retry, corrections/history/undo/privacy, encrypted backups,
  restore, settings and extension popup, including all empty/few/many/long/error/offline states.
- Verify keyboard navigation, visible focus, contrast, minimum window, text zoom and narrow
  layouts. Compare before/after using the same synthetic data and viewport, not unmatched mocks.

## Delivery sequence and evidence mapping

The Next task remains singular in PROJECT_STATUS and has Roadmap acceptance/verification.
The full Goal includes every later task; slices do not redefine completion.

| Stage | User-visible acceptance | Roadmap coverage |
| --- | --- | --- |
| Safe reading foundation | Import/Save becomes durable analyzed context with close sources in A; no-key queue and restart remain usable | TASK-005 enabling slices; existing TASK-001–004 regressions |
| Linked personal library | Automatic spaces/links/Core Self/inferred values and tacit knowledge have provenance, correction, history and undo | TASK-005–006 |
| Trust and discovery | Relevant context is globally findable and safely used with explainable destination/sensitivity decisions | TASK-007–008 |
| Durable complete library | Encrypted one-artifact restore, distinct deletion, canonical graph and explicit redacted sharing work | TASK-009–011 |
| Real everyday use | Owner-selected Save/Import→approved analysis→restart→sources→Use reduces repeated explanation | TASK-012 |
| Ready Windows handoff | Verified package/setup assets, clear limitations, repository integrated; public decisions remain explicit | TASK-013–014 |

Coverage checklist (unverified until exact evidence is linked):

- Import or explicit Save → immediate local permanent source → automatic durable analysis →
  required grounded Conversation Brief and zero or more supported items. Zero is valid.
- Search/explore → relevant context → original evidence, with global keyword/semantic retrieval,
  source/date/title/project/topic/type filtering and cross-space/exact-identifier evaluation.
- Explicit bounded allowed Use; preserve existing draft, prevent duplicate insertion/refresh,
  automatic submission and automatic Save. Clarify new-chat/completed-chat provider constraints.
- Automatic linking/classification/Core Self, inferred values/tacit knowledge, context selection,
  source/rationale/confidence visibility, editable spaces and remembered corrections.
- Versioned corrections, authority precedence, history, true undo, contradictions/expansion/stale
  handling; never silently overwrite a corrected item during reanalysis.
- No key, offline, model errors, termination/restart, queue interruption: preserve data/status,
  retry safely, expose progress and user-visible estimated use with enforceable safe limits.
- Analysis modes Auto/Project/Learning/Research-Writing/Context Handoff, visible versioned
  system prompts, additive instructions, injection-resistant golden evaluation.
- Sensitivity, inferred scope and user corrections, private/work/client/shared/unknown trust,
  cross-scope rules, explicit sensitive use, exception Review, risk-aware export warnings.
- Encrypted versioned local backup and complete one-file restore excluding protected API keys;
  preserve source metadata/evidence on source deletion, separate explicit derived deletion.
- Canonical graph shares links with Home/Explore/retrieval, node-to-source navigation, meaningful
  discovery; only explicitly selected/redacted/previewed/confirmed image sharing.
- Local diagnostics without raw content; explicit redacted diagnostic export.
- Usable optional-backfill onboarding, no-key capture/search, model discovery/recommendation,
  saved OS credentials, extension installation/Save/Use instructions and package assets.
- Full keyboard/accessibility, long text, empty/small/large data, errors/offline and responsive A.

Evidence classes: automated invariants, isolated browser outcomes, executable/package results,
installed native/browser integration, actual private-data value. Never infer a higher class from
a lower one. All 17 Product Spec acceptance scenarios and its full functional/nonfunctional
requirements remain mandatory where applicable, not just the checklist above.

Final real-use must include an actual selected conversation, approved analysis, app restart,
source verification and Use in a next chat, then owner assessment of less repeated explanation.
If a mandatory item is unverified or a significant defect remains, keep the overall Goal open.

## Build, integration and handoff

- Preserve unrelated changes; intentional English functional commits only.
- After frontend changes, run `npm run build` in frontend before packaging assets.
- Always fresh `.venv/Scripts/pyinstaller.exe --noconfirm --clean packaging/Reweave.spec`
  before implementation completion/final commit; verify `dist/Reweave/Reweave.exe` and
  `ReweaveNativeHost.exe` with appropriate isolated smokes. Build is not native install proof.
- Update affected canonical documents in the same task and run installed
  `validate_project_docs.py --root . --strict full`; perform Full Goldfish after convergence.
- Feature branch defaults `codex/`; push → PR to dev → required checks → merge commit into dev →
  merge verified dev into main with a merge commit and push → re-observe local/remote tips.
  Never stage unrelated files, target a PR at main, squash, bypass checks, or publish externally.
- Final handoff: executable location, installation/run steps, A before/after, tested evidence,
  remaining limitations and the minimum manual checks the owner actually needs.

The earlier design task changed no application source/canonical gate and used synthetic seeded
baseline data only. It did not verify current whole-product tests, extraction quality or real use.
