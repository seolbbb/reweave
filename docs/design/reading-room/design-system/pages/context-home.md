# Context Home

Extends ../MASTER.md. Visual source: ../../direction-a.png.

Use a compact header, persistent search and secondary Import action. Lead with a recent source/Brief, a readable title and the user's goal. Follow with decisions and lessons separated by whitespace and thin dividers. Avoid the current oversized container with unused grid slots.

Keep source links close to the relevant entry and observation/inference labels quiet but readable. Put open questions, the next step and related context in the wide-screen companion column; stack them at narrower widths. Avoid duplicating every scope as both chips and another navigation list.

The reuse strip explains the explicit extension flow. Label an instruction-only action 'How to use in chat'. It must not automatically inject, analyze, save or submit.

Use 16px/1.55 sans-serif body copy and short 30-32px editorial titles at normal desktop size. Allow wrapping and growth at larger text sizes. The generated image's serif body and larger headline do not override these written rules.

Required follow-on states: first run, no sources, saved but not analyzed, no key, pending/running/failed analysis, Brief with zero Context Items, populated library, many items, long content, unavailable sources, correction/undo, privacy boundaries and keyboard navigation.

